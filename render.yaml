services:
  - type: web
    name: suggestly-elite-business-platform
    runtime: node
    buildCommand: npm run build
    startCommand: ./start.sh
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    healthCheckPath: /
    autoDeploy: true
