/**
 * Components Index
 * 
 * Centralized export for all components in the src/components directory.
 * This enables clean imports like: import { Logo } from '@/components'
 */

export { default as Logo, type LogoProps } from './Logo';

// Add future components here as they are created
// export { default as Header } from './Header';
// export { default as Footer } from './Footer';