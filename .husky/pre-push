#!/usr/bin/env sh. "$(dirname -- "$0")/_/husky.sh"echo "Running Prettier checkâ€¦"npm run -s format:check || {  echo "âŒ Prettier check failed. Run: npm run format"  exit 1}if [ -f "package.json" ] && cat package.json | grep -q "\"test\""; then  echo "Running testsâ€¦"  npm test --silent || {    echo "âŒ Tests failed. Fix tests before pushing."    exit 1  }fiecho "âœ… Pre-push checks passed."